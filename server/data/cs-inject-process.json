{
  "_key": "cs-inject-process",
  "name": "Inject Process",
  "tactic": "TA0004",
  "icon": "🔀",
  "description": "Inject beacon into target process PID via Cobalt Strike",
  "executionType": "cobalt_strike",
  "estimatedDuration": 10,
  "inputs": [
    {
      "id": "session",
      "label": "Beacon Session",
      "type": "session",
      "required": true
    }
  ],
  "outputs": [
    {
      "id": "injected_session",
      "label": "Injected Session",
      "type": "session",
      "description": "Session running inside target process"
    }
  ],
  "parameters": [
    {
      "id": "session",
      "label": "Session",
      "type": "string",
      "required": true,
      "placeholder": "${CURRENT_SESSION}",
      "default": "${CURRENT_SESSION}"
    },
    {
      "id": "pid",
      "label": "PID",
      "type": "string",
      "required": true,
      "placeholder": "${EXPLORER_PID}",
      "default": "${EXPLORER_PID}"
    },
    {
      "id": "listener",
      "label": "Listener",
      "type": "string",
      "required": true,
      "placeholder": "${HTTP_LISTENER}",
      "default": "${HTTP_LISTENER}"
    },
    {
      "id": "arch",
      "label": "Arch",
      "type": "select",
      "required": false,
      "options": [
        "x64",
        "x86"
      ],
      "default": "x64"
    }
  ],
  "requirements": {
    "c2Server": true,
    "listeners": [
      "HTTP"
    ],
    "payloads": [],
    "libraries": [
      "cobaltstrikec2/cobaltstrike.py"
    ]
  },
  "robotFramework": {
    "keyword": "Inject Process",
    "libraries": [
      "cobaltstrikec2/cobaltstrike.py"
    ],
    "keywordArgs": [
      {
        "param": "session",
        "position": 1,
        "sessionVariable": "CURRENT_SESSION"
      },
      {
        "param": "pid",
        "position": 2,
        "variableRef": false
      },
      {
        "param": "listener",
        "position": 3,
        "variableRef": true,
        "variableName": "HTTP_LISTENER"
      },
      {
        "param": "arch",
        "position": 4,
        "variableRef": true,
        "variableName": "ARCH"
      }
    ],
    "preKeywordLog": "=== Injecting into PID: ${pid} ===",
    "documentation": "Process injection via beacon",
    "tags": [
      "privilege-escalation",
      "injection"
    ]
  }
}